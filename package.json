{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "next build && next export -o docs &&git checkout -- docs/.nojekyll && git add docs && git commit -m \"auto: regen site\" && git push origin main"
  },
  "dependencies": {
    "@chakra-ui/icons": "^1.0.0-rc.7",
    "next": "9.5.5",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "react-icons": "^3.11.0"
  },
  "devDependencies": {
    "@chakra-ui/core": "^1.0.0-rc.7",
    "framer-motion": "^2.9.4"
  }
}
